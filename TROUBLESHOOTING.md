# 故障排除指南

## 常见问题

### 1. kornia 提示安装 basicsr 依赖

**问题描述：**
安装依赖时，kornia 可能会提示：
```
Optional dependency 'basicsr' is not installed. 
Do you wish to install the dependency? [Y]es, [N]o, [A]ll.
```

**原因：**
- basicsr 与 torch 2.8.0 及更高版本不兼容
- basicsr 的构建过程中找不到 torch 模块

**解决方案：**
1. **选择 `[N]o` 或 `[A]ll`（自动）** - 本插件已配置自动跳过此依赖
2. **环境变量已自动设置** - 插件在加载时会设置：
   - `KORNIA_INSTALL_MODE=auto`
   - `KORNIA_CHECK_DEPS=0`
   - `BASICSR_JIT=0`
3. **无需手动安装 basicsr** - 本插件不需要 basicsr 功能

**验证：**
如果仍然看到提示，请确保：
- 使用最新版本的插件代码
- 重启 ComfyUI 以应用环境变量设置

### 2. 内存清理后模型无法加载

**问题描述：**
使用深度内存清洗节点后，后续加载模型出现问题。

**解决方案：**
- 深度内存清洗会完全卸载所有模型，确保 ComfyUI 恢复到刚启动状态
- 这是正常行为，后续加载模型会重新初始化
- 建议在工作流末尾或需要完全释放内存时使用此节点

### 3. URL 资源加载失败

**问题描述：**
从 URL 加载图片/视频/音频失败。

**可能原因和解决方案：**
1. **网络连接问题** - 检查网络连接和防火墙设置
2. **URL 格式错误** - 确保 URL 完整且有效
3. **资源不存在** - 验证 URL 可在浏览器中访问
4. **认证问题** - 某些资源可能需要认证（如阿里云 OSS 的签名 URL）

### 4. GPU 内存未完全释放

**问题描述：**
使用内存清洗节点后，GPU 显存显示仍然占用。

**解决方案：**
- GPU 的 "reserved" 内存是正常的，CUDA 会预留一部分内存池
- 关注 "allocated" 内存，这才是实际使用的显存
- 如需完全释放，重启 ComfyUI 是最彻底的方式

## 需要更多帮助？

如果以上方案无法解决您的问题，请：
1. 检查 ComfyUI 日志输出
2. 在 GitHub 项目页面提交 Issue
3. 提供详细的错误信息和环境信息（Python 版本、torch 版本等）
