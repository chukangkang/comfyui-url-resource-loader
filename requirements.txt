# ===================== URL加载节点扩展依赖 =====================
requests>=2.31.0          # HTTP请求（加载URL图片/音频/视频核心依赖）
typing-extensions>=4.5.0  # 提供override装饰器（扩展类必需）
tempfile                  # Python内置，无需安装，仅标注
hashlib                   # Python内置，无需安装，仅标注

# ===================== 可选优化依赖（按需启用）=====================
# 视频处理增强（若需更完善的URL视频解码）
ffmpeg-python>=0.2.0
# 音频兼容优化（解决torchaudio解码异常）
soundfile>=0.12.1
